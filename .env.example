# Development Environment Variables for Server
# Copy this file to .env if you need custom configuration

# Database Configuration (Optional - defaults will work)
POSTGRES_USER=warehouse_user
POSTGRES_PASSWORD=dev_password_123
POSTGRES_DB=warehouse_db
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=8080

# Note:
# - For dev server, you can use deploy.sh directly without .env file
# - Default credentials in docker-compose.yml will work fine
# - Only create .env if you need custom configuration
